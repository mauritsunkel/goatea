% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_splitdot.R
\name{plot_splitdot_combined}
\alias{plot_splitdot_combined}
\title{Plot combined splitdot plot across GO sources}
\usage{
plot_splitdot_combined(
  enrichment,
  topN = 5,
  highlight_terms = NULL,
  mode = "gene_ratio",
  show_signif_overlay = TRUE
)
}
\arguments{
\item{enrichment}{GOAT enrichment result containing multiple GO sources}

\item{topN}{Integer. Number of terms per source. Slots filled first by \code{highlight_terms}
matches, then by top terms ranked by adjusted p-value. Default: 5.}

\item{highlight_terms}{Character vector of term names or IDs to prioritize across all sources.
Matched on \code{name} or \code{id} columns. Default: \code{NULL}.}

\item{mode}{Character. Either \code{"gene_ratio"} (default) or \code{"pvalue"}.
Controls x-axis and color legend only, same as \code{\link{plot_splitdot}}.}

\item{show_signif_overlay}{Logical. When \code{TRUE}, adds an inner black dot for
\code{ngenes_signif} on the same size scale as \code{ngenes}. Default: \code{TRUE}.}
}
\value{
ggplot2 object. \code{attr(p, "suggested_width")} contains a suggested render width
in inches — see \code{\link{plot_splitdot}}.
}
\description{
Shows enrichment terms from GO:BP, GO:CC, and GO:MF in a 3-row x 2-column grid
(rows = source, columns = Upregulation/Downregulation). Within each source, highlighted
terms are shown first (top), followed by auto-filled top terms by adjusted p-value.
}
\examples{
# Build multi-source data from the single-source example by assigning GO source labels
enr <- get(load(system.file("extdata", "example_enrichment.rda", package = "goatea")))
multi_enr <- rbind(
  transform(enr, source = "GO:BP"),
  transform(enr, source = "GO:CC"),
  transform(enr, source = "GO:MF")
)
plot_splitdot_combined(multi_enr, topN = 5, show_signif_overlay = FALSE,
  highlight_terms = c("geneset name 7", "geneset name 3"))
}
